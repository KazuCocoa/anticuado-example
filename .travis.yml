language: objective-c
osx_image: xcode7.3
cache: bundler
rvm:
  - 2.3.0

before_install:
  - brew update
  - brew install elixir
  - gem install bundler -v 1.12.5
  - bundle install

script:
  - mix local.hex --force
  - bundle exec ruby main.rb

